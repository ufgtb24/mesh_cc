#pragma once
#include <string>
#include <fstream>
#include <vector>
#include <iostream>
using namespace std;

int load_file(string file_name, int* adj, int K);
int load_file(string file_name, float* adj, int K);


